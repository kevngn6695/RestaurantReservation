import React from "react";
import Container from "./Container";
import Wrapper from "./Wrapper";

function VirtualTable(props) {
  return <Wrapper className="virt-table-wrpr"></Wrapper>;
}

export default React.memo(VirtualTable);
